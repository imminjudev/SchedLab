# Generated Results

Experiment datasets are generated under this directory and are ignored by Git.

Accepted datasets must record source commit, kernel version, scheduling policy,
workload configuration, execution order, CPU topology, and toolchain versions.
